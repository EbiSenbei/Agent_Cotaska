---
id: T-0359
title: 調査：チャットの応答速度が速くならないか？
status: todo
priority: normal
progress_status: 仕掛
due_date: '2026-07-02'
deadline_date: null
list: 仕様変更
parent: T-0157
tags: []
sort_order: 349
delete_flag: 0
created_at: '2026-07-01T10:30:41.001Z'
updated_at: '2026-07-04T00:25:02+09:00'
completed_at: null
deleted_at: null
---

Codexと比べてチャットの応答速度が遅いと感じる。
Codexと比較して応答速度が上がるナニカがないか？調査してほしい

## 調査結果

結論として、Cotaska側だけで「モデルの実処理時間」を大きく短縮する余地は限定的。
ただし、体感速度の改善と、入力トークン削減による実処理時間の短縮余地はある。

### 追加タスク

- T-0376: `ai_messages.token_count` で `cached_input_tokens` を二重計上している可能性があるため、不具合修正タスクとして追加。
- T-0377: 速度優先モードではCodexスレッドを継続しない、または一定条件でローテーションする仕様変更タスクとして追加。

### 現状確認

- CotaskaのAIチャットは `@openai/codex-sdk` をElectron main processから呼び出している。
- `20_app/src/main/codexSdkService.js` では `codexThread.runStreamed(prompt, { signal })` を使用しており、SDKのストリーミングイベント自体は受信している。
- 受信したイベントは `mainWindow.webContents.send("aiChat:runEvent", payload)` でrendererへ通知している。
- renderer側の `AiChatPane.jsx` では、処理中カードにイベント一覧として表示している。
- 一方で、最終的なassistantメッセージ本文は、処理完了後に `ai_messages` へ保存されたものを画面へ反映している。

### 遅く感じる主な理由

1. モデル側の推論時間
   - Codexはコーディング向けに推論・作業計画・ファイル確認を行うため、通常の短文チャットより応答に時間がかかる。
   - 複雑な依頼、長い参照ファイル、大きな作業フォルダではさらに遅くなる。

2. 参照ファイルによる入力増加
   - Cotaskaは選択された参照ファイルをプロンプト先頭に展開している。
   - 現在の初期値は「最大10ファイル、合計100,000文字」。
   - 参照が多いほど送信前処理、入力トークン、モデル処理時間が増える。

3. 体感速度の問題
   - 途中イベントは表示されるが、assistant本文そのものは完了後に反映される。
   - そのため、Codexアプリのように「文章が増えていく」感覚と比べると遅く感じやすい。

4. 計測値が不足している
   - 現状の `ai_runs` は開始・終了・エラーを保持できるが、以下のような細かい時刻は持っていない。
     - 送信開始
     - SDKロード完了
     - Codex thread開始/再開完了
     - 初回イベント受信
     - 初回agent_message受信
     - 最終応答保存完了
   - そのため、遅さの原因が「SDK起動」「認証/通信」「参照ファイル読み込み」「モデル推論」「UI反映」のどこかを定量的に判断しにくい。

## 公式情報から見た速度改善候補

Codex公式マニュアル上は、速度面で以下の選択肢がある。

参照元:

- OpenAI Codex Manual: `https://developers.openai.com/codex/codex-manual.md`
- 確認箇所: Speed、Codex SDK、Model selection

- Fast mode
  - 対応モデルの速度を上げる代わりにクレジット消費が増える。
  - CLIでは `/fast on`、設定では `service_tier = "fast"` と `[features].fast_mode = true` を使う方針が示されている。
  - ChatGPTサインイン時のCodex向け機能であり、APIキー利用時は標準API課金扱いになる。

- 軽量モデル
  - 公式マニュアルでは、重い作業には推奨モデル、軽い作業には `gpt-5.4-mini` のような高速・低コスト寄りモデルを使う考え方が示されている。
  - ただし、Cotaskaでは現時点でモデル選択UIを不要としているため、導入するなら設定画面側の任意項目として扱うのがよい。

- Codex-Spark
  - 近い将来の高速反復向け選択肢として公式マニュアルに記載がある。
  - ただし利用条件や提供範囲に制約があるため、Cotaskaの標準前提にはしない。

## 推奨対応

### 優先度高

1. 応答時間の計測ログを追加する
   - `ai_runs` に計測用カラムを追加する、または `ai_run_events` を追加する。
   - 最低限、以下を記録する。
     - `sent_at`
     - `sdk_loaded_at`
     - `thread_started_at`
     - `first_event_at`
     - `first_message_at`
     - `completed_at`
   - これにより「どこが遅いか」を次回以降に判断できる。

2. assistant本文の逐次表示を強化する
   - 現在は処理中カードへイベント表示しているが、`agent_message` の `item.text` を一時assistantメッセージとして表示・更新する。
   - 完了時にDB保存済みの最終assistantメッセージへ差し替える。
   - 実処理時間は変わらないが、体感速度は大きく改善しやすい。

3. 参照ファイル量を見える化する
   - 送信時に「参照ファイル数」「合計文字数」「切り詰め有無」を画面または実行ログへ表示する。
   - 重い場合はユーザーが参照を減らせるようにする。

### 優先度中

4. 設定画面に速度優先オプションを追加する
   - 例: `標準` / `速度優先`
   - 実装候補:
     - Codex config側のFast mode設定を利用できるか確認する。
     - SDKのthread optionまたは環境設定で `service_tier` 相当を渡せるか確認する。
   - SDKのTypeScript APIでFast modeを直接指定できるかは、実装前にSDKリポジトリ/型定義の確認が必要。

5. 軽量モデル選択を設定項目として追加する
   - ユーザー要望としてチャット入力欄のモデル選択ボタンは不要。
   - 追加するなら設定画面に「AIモデル（空ならCodex既定）」として置くのがよい。
   - ただしモデル一覧の取得仕様は別途調査が必要。

### 優先度低

6. Codex SDKインスタンスやthread再開の最適化を検討する
   - 現状は送信ごとに `new Codex()` している。
   - 効果があるかは不明なため、計測後に判断する。
   - 安易に永続化すると認証状態、キャンセル、複数スレッド同時送信、アプリ終了処理が複雑になる。

## 後続タスク候補

- AI応答時間の計測ログを追加する。
- `agent_message` の途中本文をチャット本文として逐次表示する。
- 参照ファイル数・文字数・切り詰め状態を送信前/処理中に表示する。
- Fast modeまたは速度優先設定をCodex SDKから利用できるか追加調査する。
- 設定画面に任意のモデル指定欄を追加するか検討する。

## 結論

すぐに効果が見込めるのは、実速度そのものよりも体感速度の改善。
特に `agent_message` の途中本文表示と、参照ファイル量の見える化を優先するのがよい。
そのうえで計測ログを入れ、実際に遅い箇所がSDK起動なのか、参照ファイルなのか、モデル推論なのかを切り分ける。

## 追加対応

調査用に、AIチャット送信時とAI応答完了時のログ出力を追加した。

対象:

- `20_app/src/main/codexSdkService.js`

ログ出力先:

- 既存の `appLogger` を使用する。
- `logs/app-YYYY-MM-DD.log` に `AI chat request started`、`AI chat assistant response`、`AI chat canceled`、`AI chat failed` として出力する。

出力内容:

- `thread_id`
- `run_id`
- `codex_thread_id`
- `request_id`
- `sandbox`
- `workdir`
- `model`
- `prompt_chars`
- `user_input_chars`
- `reference_files`
- `reference_chars`
- `reference_skipped`
- `response_chars`
- AI応答本文全文 `response`
- `usage`
- `timings_ms`
  - total
  - open_ai_service_to_sdk_loaded
  - sdk_loaded_to_stream_started
  - stream_started_to_first_event
  - stream_started_to_first_agent_message
  - stream_started_to_completed

注意:

- AI応答本文全文がログに残るため、調査目的のログとして扱う。
- ログ出力に失敗してもAIチャット本体は止めない。

## 追加調査: Codex直接利用との速度差

同じ質問をCotaskaのAIチャットとCodex直接利用で比較した。

質問:

```text
今の時刻を教えてください。
また、この処理にかかった時間も図ってください
```

### Cotaska側の実測

Cotaskaの `data/ai.sqlite` を確認した。

対象run:

- `run_id`: `run_c397b951-796c-4fc2-8fd9-290fadc0f477`
- `thread_id`: `thread_fa23b2e5-c891-41cc-89d9-78d18ac5dfa9`
- `codex_thread_id`: `019f1d5a-bded-7423-82d4-0e3b0e9ce66d`
- sandbox: `danger-full-access`
- started_at: `2026-07-01T14:52:59.302Z`
- finished_at: `2026-07-01T14:53:25.434Z`

差分:

- 約 `26.132秒`

同じrunに紐づくメッセージ:

- user message created_at: `2026-07-01T14:52:59.304Z`
- assistant message created_at: `2026-07-01T14:53:25.436Z`

### Codex直接利用側の実測

ユーザー確認では、Codex直接利用では同じ質問が約 `5秒` で完了した。

### 該当Cotaskaスレッドの状態

Cotaska側の該当スレッドには、参照ファイルとして以下が付いていた。

- `data\tasks\T-0079.md`

ただし、このファイルは約328文字/20行程度であり、今回の26秒差の主因と断定するほど大きくはない。

スレッド履歴も以下の程度で、極端に大きくはなかった。

- user: 2件 / 約48文字
- assistant: 2件 / 約114文字

## 速度差の推定原因

現時点では、以下の複合要因と考える。

1. CotaskaはCodex SDK経由で呼び出している
   - Electron main processから `@openai/codex-sdk` を呼び、結果をSQLiteへ保存し、rendererへ反映している。
   - Codex直接利用より処理経路が長い。

2. Cotaskaは毎回 `new Codex()` している
   - `codexSdkService.sendMessage()` 内で送信ごとに `new Codex()` している。
   - SDK内部の初期化、接続、セッション準備が毎回発生している可能性がある。
   - ここが遅いかどうかは、追加した詳細ログで `open_ai_service_to_sdk_loaded` と `sdk_loaded_to_stream_started` を確認する必要がある。

3. Cotaskaは作業フォルダ・sandbox・参照ファイル付きの開発作業コンテキストで実行している
   - 単純な時刻質問でも、Codex SDK側では作業フォルダ付きのスレッド実行として扱われる。
   - Codex直接利用側の計測表示とは、内部で同じ条件ではない可能性がある。

4. 計測範囲が完全には一致していない
   - Cotaskaの26.132秒は、`ai_runs.started_at` から `ai_runs.finished_at` まで。
   - Codex直接利用の5秒は、画面に表示された作業時間。
   - どちらも参考値として有用だが、完全に同一の計測点ではない。

5. 追加ログがまだ有効な状態で再計測されていない
   - AI応答ログ追加後、Cotaska実行中のmain processが古いままだった可能性がある。
   - `logs/app-YYYY-MM-DD.log` には23時台の `AI chat assistant response` が出ていなかった。
   - 正確な内訳を見るには、Cotaskaを再起動してから再計測する必要がある。

## 追加の改善案

### 優先度高

1. Cotaska再起動後に詳細ログで再計測する
   - 追加済みの `AI chat request started` / `AI chat assistant response` ログで、以下を確認する。
     - `open_ai_service_to_sdk_loaded`
     - `sdk_loaded_to_stream_started`
     - `stream_started_to_first_event`
     - `stream_started_to_first_agent_message`
     - `stream_started_to_completed`
   - これにより、SDK初期化が遅いのか、Codex実行自体が遅いのかを切り分ける。

2. Codex SDKインスタンスの使い回しを検討する
   - 現状は送信ごとに `new Codex()`。
   - main process内でCodexインスタンスを保持し、送信ごとに再利用できるか検証する。
   - 期待効果:
     - SDK初期化時間の削減
     - 認証/接続準備の再実行削減
   - 注意点:
     - 複数スレッド同時送信
     - キャンセル処理
     - アプリ終了時の破棄
     - 認証状態変更時の再初期化

3. 単純質問では参照ファイルを送らない/軽くする設定を追加する
   - 現在は参照ファイルが付いていれば、質問内容に関係なくプロンプト先頭へ展開する。
   - 速度優先時は以下を選べるとよい。
     - 参照ファイルを送らない
     - 明示的に選択した場合だけ送る
     - 「このファイルを見て」などの文脈がある場合だけ送る

### 優先度中

4. 入力欄または設定に「速度優先」モードを追加する
   - 速度優先時の候補:
     - 参照ファイル自動展開を抑制
     - 軽量モデルを利用
     - Fast mode相当がSDKから指定可能か確認
   - Codex直接利用と同じFast mode/モデル設定をCotaska SDK経由で指定できるかは追加調査が必要。

5. `agent_message` の途中本文をチャット欄へ逐次表示する
   - 実時間が短くならなくても、体感速度は改善しやすい。
   - Codex直接利用に近い体験にするには、処理中カードだけでなく本文が伸びる表示が望ましい。

### 優先度低

6. 単純な一般質問をCodex SDKではなく軽量なAI経路へ分ける
   - ただしCHG-058の現行方針はCodex SDK利用。
   - AIプロバイダ拡張や一般チャット用途を広げる段階で検討する。

## 追加結論

今回の比較では、Cotaska側はCodex直接利用より明確に遅い。
まずは追加ログを有効化した状態で再測定し、遅延の内訳を確認する。

改善の第一候補は以下。

1. Codex SDKインスタンスの使い回し
2. 単純質問時の参照ファイル送信抑制
3. `agent_message` の途中本文表示
4. 速度優先設定の追加

この順で対応すると、実速度と体感速度の両方を改善しやすい。

## 子タスク

- T-0364 調査: AIチャット応答速度を詳細ログで再計測する
- T-0365 実装: Codex SDKインスタンスの使い回しを検討・実装する
- T-0366 実装: 単純質問時の参照ファイル送信抑制を追加する
- T-0367 調査・実装: 速度優先モードとFast mode利用可否を整理する
- T-0368 実装: agent_messageの途中本文をチャット欄へ逐次表示する
- T-0369 調査: AIチャット入力トークン肥大化の原因を確認する
- T-0370 調査・実装: 速度優先時の推論強度設定を検証する
- T-0371 実装: 速度優先モード用の軽量Codex実行プロファイルを追加する

## 追加検証: 速度優先モード実装後の実測

2026-07-02 01時台に、速度優先モードを有効にした状態で再検証した。

質問:

```text
今の時刻を教えてください。
また、この処理にかかった時間も図ってください
```

対象ログ:

- `logs/app-2026-07-01.log`
- `data/ai.sqlite`

対象run:

- `run_id`: `run_1c1860b5-fc83-4237-8dbc-cca99787bb62`
- `thread_id`: `thread_08a5e8d2-6b0b-4f35-b908-34f87a07be15`
- `codex_thread_id`: `019f1e7e-cbb0-77b3-a49e-238deeb3b960`
- sandbox: `danger-full-access`
- performance_mode: `speed`
- fast_mode_requested: `true`
- 参照ファイル: 1件
- 参照文字数: 328文字

### 実測結果

アプリログ上の処理時間:

- 合計: `12,391 ms`（約12.4秒）
- SDK読み込みまで: `17 ms`
- SDK読み込み後からstream開始まで: `3 ms`
- stream開始から初回イベントまで: `1,124 ms`
- stream開始から初回agent_messageまで: `5,752 ms`
- stream開始から完了まで: `12,371 ms`

DB上の `ai_runs`:

- started_at: `2026-07-01T16:24:13.311Z`
- finished_at: `2026-07-01T16:24:25.693Z`
- 差分: 約 `12.382秒`

トークン使用量:

- input_tokens: `29,944`
- cached_input_tokens: `19,712`
- output_tokens: `258`
- reasoning_output_tokens: `60`

### 読み取り

- 速度優先モードの設定はログ上 `performance_mode: speed` / `fast_mode_requested: true` として渡っている。
- 以前の詳細ログ計測では約 `15.225秒` だったため、今回の約 `12.4秒` は少し改善している。
- ただし今回の計測では参照ファイルが1件付いており、入力トークンも約3万あるため、完全な同条件比較ではない。
- SDK読み込みやstream開始までは非常に短く、主な待ち時間はCodex側の実行・推論・応答生成にある。
- 画面上のAI回答に出ていた「約87.3ミリ秒」は、AIが回答内で測った時刻取得処理の時間であり、CotaskaからCodex SDKへ送って応答を保存するまでの全体時間ではない。

### 現時点の評価

- 速度優先モードはCotaskaからCodex SDKへ意図通り要求できている。
- 体感速度は `agent_message` の途中本文表示により改善している。
- 実時間は大幅短縮ではなく、数秒程度の改善に留まっている。
- 今後さらに詰めるなら、次は同条件で以下を比較する。
  - 標準 / 速度優先
  - 参照ファイルあり / なし
  - 新規スレッド / 既存スレッド

### ログ方針の補足

当初は調査目的でAI応答本文全文をログに残す案だったが、現在は安全性を優先し、応答本文はログに残さない方針へ修正済み。

現在ログに残す主な情報:

- `response_chars`
- `usage`
- `timings_ms`
- `reference_context`
- `performance_mode`
- `fast_mode_requested`

## 追加検証: 2026-07-02 参照ファイルなし・速度優先ON

2026-07-02 11時台に、設定の `AI速度` が保存されることをユーザー確認後、参照ファイルなしで再検証した。

質問:

```text
今の時刻を教えてください。
また、この処理にかかった時間も図ってください
```

対象ログ:

- `logs/app-2026-07-02.log`
- `data/ai.sqlite`

対象run:

- `run_id`: `run_57e5f1fa-f3b4-4f5d-bb61-867ce7421176`
- `thread_id`: `thread_8a085bde-c20b-4ab9-9553-3ed8718a0b1f`
- `codex_thread_id`: `019f209e-17cf-7023-81e0-3c2d87c6e259`
- sandbox: `danger-full-access`
- performance_mode: `speed`
- fast_mode_requested: `true`
- 参照ファイル: 0件
- prompt_chars: 36

### 実測結果

アプリログ上の処理時間:

- 合計: `29,258 ms`（約29.3秒）
- SDK読み込みまで: `256 ms`
- SDK読み込み後からstream開始まで: `45 ms`
- stream開始から初回イベントまで: `13,784 ms`
- stream開始から初回agent_messageまで: `19,070 ms`
- stream開始から完了まで: `28,957 ms`

DB上の `ai_runs`:

- started_at: `2026-07-02T02:17:25.958Z`
- finished_at: `2026-07-02T02:17:55.204Z`
- 差分: 約 `29.246秒`

トークン使用量:

- input_tokens: `29,529`
- cached_input_tokens: `20,224`
- output_tokens: `215`
- reasoning_output_tokens: `17`

### 読み取り

- 参照ファイルなし・速度優先ONでも、今回の実測は約29.3秒で、前回の約12.4秒より遅かった。
- SDK読み込みとstream開始までは短く、遅延の大半は `stream_started_to_first_event` と `stream_started_to_first_agent_message` に集中している。
- つまり、今回もCotaska側の保存処理やSDK初期化より、Codex側の応答開始待ちが主な待ち時間と考えられる。
- 画面上のAI回答に出ていた `62.172 ms` は、AIが回答内で測った時刻取得処理の時間であり、CotaskaからCodex SDKへ送って応答保存されるまでの全体時間ではない。
- Fast mode要求はログ上 `fast_mode_requested: true` として渡っているが、実時間はrunごとのばらつきが大きい。

### 次の確認観点

- 同じ条件で標準/速度優先を連続比較し、速度優先が安定して効いているか確認する。
- 新規スレッド/既存スレッドで応答開始待ちが変わるか確認する。
- 入力トークンが参照なしでも約3万あるため、Codex SDK側の基礎コンテキストや作業フォルダ情報がどの程度含まれているかを追加調査する。

## 追加検証: 2026-07-03 同一スレッド1回目/2回目比較

同一AIスレッドで、時刻質問を2回連続投稿して比較した。

対象:

- `thread_id`: `thread_47047f4c-e3fc-4bd7-8ddc-78814b3ea551`
- `codex_thread_id`: `019f285e-0dbe-7383-952d-1f3626efff9e`
- performance_mode: `speed`
- fast_mode_requested: `true`
- 参照ファイル: 0件

### 実測結果

| 回 | run_id | input_tokens | cached_input_tokens | total | 初回イベント | 初回AI本文 |
|---|---|---:|---:|---:|---:|---:|
| 1回目 | `run_9f44d4e2-98a7-4820-93cb-72a94792f9eb` | 29,522 | 24,320 | 22.888秒 | 8.519秒 | 16.433秒 |
| 2回目 | `run_48ab6478-a758-4741-92de-2970b7948dd3` | 59,604 | 49,152 | 10.016秒 | 1.140秒 | 3.862秒 |

### 読み取り

- 2回目は履歴が乗るため `input_tokens` は約2倍に増えた。
- ただし `cached_input_tokens` も約2倍に増え、応答開始は大きく速くなった。
- 新規スレッド1回目は基礎文脈の構築/読み込みで遅く、同一スレッド2回目以降はキャッシュ効果で速くなる可能性が高い。
- 速度評価では「新規スレッド1回目」と「同一スレッド継続投稿」を分けて測る必要がある。
- 長いスレッドでは履歴増加により、いずれ再び重くなる可能性がある。

## 追加検証: 2026-07-04 軽量Codex実行プロファイル適用後

T-0371の対策後に、同一AIスレッドで時刻質問を再実行した。

対象:

- `thread_id`: `thread_47047f4c-e3fc-4bd7-8ddc-78814b3ea551`
- `codex_thread_id`: `019f285e-0dbe-7383-952d-1f3626efff9e`
- performance_mode: `speed`
- 参照ファイル: 0件

### 実測結果

ログ上の実測は **13.458秒**。

- 送信開始: `2026-07-04 00:04:50.305 JST`
- 応答保存: `2026-07-04 00:05:03.751 JST`
- `timings_ms.total`: `13458 ms`
- 初回イベントまで: `1155 ms`
- 最初のAI本文まで: `4527 ms`
- 完了まで: `13435 ms`
- `input_tokens`: `116,356`
- `cached_input_tokens`: `91,392`

軽量プロファイルの反映状況:

- `web_search_mode`: `disabled`
- `multi_agent_enabled`: `false`
- `model_reasoning_effort`: `low`
- `performance_mode`: `speed`

### 読み取り

- 直前の実測 **17.049秒** から **13.458秒** となり、約 **3.6秒改善**した。
- 初回イベントまでが `4463 ms` から `1155 ms` へ短縮した。
- 最初のAI本文までが `9856 ms` から `4527 ms` へ短縮した。
- 体感速度には改善効果が出ている可能性が高い。
- 一方で、`input_tokens` は `90,403` から `116,356` へ増加した。
- 同じ既存Codexスレッドで会話を積み上げている影響が大きいと考えられる。
- この結果を受けて、T-0377で「速度優先モードではCodexスレッドを継続しない/ローテーションする」方針を追加検討する。

## 対策方針

ここまでの調査から、応答速度改善は以下の3方向で進める。

1. Codex実行プロファイルを軽くする。
   - T-0371で、速度優先モード時に `web_search = "disabled"`、`features.multi_agent = false`、`modelReasoningEffort = "low"` を適用する。
   - 目的は、参照なし短文でも大きい基礎入力トークンを減らすこと。

2. 同一スレッド継続利用を優先する。
   - 同一スレッド2回目では `cached_input_tokens` が増え、応答開始が大きく速くなった。
   - タスク単位では、既存AIスレッドを再利用する導線を優先する。
   - 明示的な新規スレッド作成は残す。

3. 初回遅延をどう扱うか調査する。
   - 新規スレッド1回目は遅くなりやすい。
   - プリウォームで改善できる可能性はあるが、トークン消費や履歴汚染の副作用があるため、先に調査する。

追加した後続タスク:

- T-0372 調査: 新規スレッド初回と継続投稿のキャッシュ効果を測定する
- T-0373 実装: タスク単位の既存AIスレッド継続利用を優先する
- T-0374 調査: 新規AIスレッド初回遅延のプリウォーム可否を確認する
- T-0375 後処理: AI応答速度調査ログを削除または設定化する

## 調査ログの後処理方針

AI応答速度調査用ログは、後で削除または設定化しやすいよう、`AI_DIAGNOSTICS` の目印を付ける。

対象コード:

- `20_app/src/main/codexSdkService.js`

検索キーワード:

```powershell
rg "AI_DIAGNOSTICS|logAiChatDiagnostics" 20_app/src
```

方針:

- 調査中は `AI_DIAGNOSTICS` ログを残す。
- 調査完了後はT-0375で削除、または設定画面からON/OFFできる正式診断ログへ変更する。
- 既定ではAI応答本文をログに残さず、文字数・usage・timingsなどの診断情報に限定する。
