interface:
  display_name: "Cotaska Release to R2"
  short_description: "Build Cotaska and publish the latest release to R2"
  default_prompt: "$cotaska-release-r2 を使用して、Cotaskaをリリース＋クラウドフレアにアップロードしてください。"

policy:
  allow_implicit_invocation: true
